id=selinux_seqno_fix
name=MuMu SELinux Status Repair
version=v0.12
versionCode=12
author=Andrea-Lyz
description=Repair KernelSU's zeroed SELinux status.policyload so MuMu can run enforcing; does not rewrite procfs, mounts, kernel identity, or detector output.
